# Отчёт по выполнению работы Ansible

## 1. Установка и подготовка управляющей машины

**Что было сделано:**

* Обновили систему (`apt update && apt upgrade`).
* Установили Python, pip и необходимые зависимости.
* Установили Ansible через пакетный менеджер.
* Проверили установку командой `ansible --version`.


---

## 2. Создание и настройка SSH‑ключей

**Что было сделано:**

* Сгенерировали SSH ключевую пару (`ssh-keygen`).
* Настроили правильные права на ключи.



---

## 3. Развёртывание управляемого хоста в Docker

**Что было сделано:**

* Создали Dockerfile.
* Создали docker-compose.yml.
* Собрали образ и запустили контейнер.
* Проверили работу контейнера.



---

## 4. Настройка SSH‑доступа в контейнер

**Что было сделано:**

* Создали .ssh в контейнере.
* Скопировали публичный ключ в `authorized_keys`.
* Установили корректные права.
* Проверили вход по SSH без пароля.



---

## 5. Создание инвентарного файла Ansible

**Что было сделано:**

* Создали файл `inventory.ini`.
* Добавили параметры подключения.
* Проверили инвентарь командой `ansible-inventory --list`.



---

## 6. Проверка соединения Ansible с хостом

**Что было сделано:**

* Выполнили ping-модуль (`ansible -m ping`).
* Получили системные факты (`ansible -m setup`).
* Запустили пробную команду (`uname -a`).


---

## 7. Создание и запуск Ansible Playbook

**Что было сделано:**

* Написали playbook `playbook.yml`.
* Запустили его командой `ansible-playbook`.
* Проверили выполнение всех задач.



---

## 8. Выполнение заданий из методички

### Задание 1 — подключение к хосту

Выполнили:

* ping
* проверку инвентаря
* SSH вход

### Задание 2 — ad‑hoc команды

Выполнили команды для:

* проверки CPU
* просмотра диска
* списка пользователей
* смены таймзоны

### Задание 3 — работа с файлами

* Создали playbook `task3_files.yml`.
* Создали директории и файлы.
* Отобразили содержимое.



---

## 9. Основные команды для диагностики

* Проверка контейнеров: `docker-compose ps`
* SSH‑debug: `ssh -v`
* Запуск playbook с повышенной подробностью: `ansible-playbook -vvv`
* Проверка синтаксиса playbook: `ansible-playbook --syntax-check`

---

## 10. Заключение

В ходе работы была полностью настроена инфраструктура для демонстрации возможностей Ansible: от установки управляющей среды до создания управляемого Docker‑хоста, подключения к нему по SSH и выполнения playbook’ов. Были изучены базовые модули, работа с файлами, директориями и выполнение команд на удалённых системах.

**Итог:** система успешно работает, все задачи methodички выполнены.
